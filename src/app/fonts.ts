import { Inter, Epilogue } from 'next/font/google';

export const epilogue = Epilogue({
  subsets: ['latin'],
  display: 'swap',
});

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});
